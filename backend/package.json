{
  "name": "market-data-explorer-gateway",
  "version": "1.0.0",
  "description": "Bun + Express API Gateway for Market Data Explorer",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@types/http-proxy-middleware": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "http-proxy-middleware": "^4.1.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.2",
    "typescript": "^5.8.3"
  }
}
